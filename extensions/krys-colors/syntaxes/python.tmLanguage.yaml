---
injectionSelector: L:source.python -string

scopeName: source.fix.python

patterns:
  - include: "#async"

repository:
  async:
    match: |-
      \b(async)\b
    captures:
      "1":
        name: storage.modifier.async.python
